import { StyleSheet } from 'react-native';

export const onServiceScreenStyle = StyleSheet.create({
  container: {
    width: '100%',
    minHeight: '100%',
    height: 'auto',
    padding: 20,
  },
});
