import { StyleSheet } from 'react-native';

export const circleImageStyles = StyleSheet.create({
  image: {
    borderRadius: 999,
    width: '100%',
    aspectRatio: 1,
    objectFit: 'cover',
  },
});
