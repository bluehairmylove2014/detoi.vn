import { StyleSheet } from "react-native";

export const LoadingIndicatorSectionStyle = StyleSheet.create({
    container: {
        display: 'flex',
        flexDirection: 'column',
        justifyContent: "center",
        alignContent:"center",
        gap: 13
    },
})