import { IReviewBase } from '@business-layer/services/entities';

function ReviewCard({ review }: { review: IReviewBase }) {
  return <div className='w-full h-full bg-white '></div>;
}

export default ReviewCard;
